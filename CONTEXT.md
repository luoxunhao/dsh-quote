# dsh-quote — 领域词汇表

本文件只记录领域术语（纯词汇，无实现细节）。实现决策见 `docs/adr/`。

> 溯源：dsh-quote 是一个 DSH 极简工具——在对话里划选一段文字，在弹出菜单里「添加到对话」，让这段文字随你的下一条消息进入对话。

## 核心概念

- **引文（Quote）**：用户在某条对话消息（assistant / 用户自己 / tool 输出，任意）渲染正文上划选并选中（右键菜单「添加到对话」）的一段文字。它是本插件操作的原子单位。
- **添加到对话（Quote to context）**：划选菜单里的动作文案。把选中引文登记为该会话的一条"待生效引用"。
- **待生效引用（Pending quote）**：用户已「添加到对话」但尚未发送的一段引文，排在该会话里等待下一次用户消息。以附件卡片显示在输入框卡片内，可移除。
- **引文消息（Quote message）**：一条引文被投递给模型时的形态——**一条独立的、普通 `user` 角色的消息**，正文就是引文原文，排在用户自己那条消息**之前**。转录区因此把它渲染成一个正常的用户气泡。
- **已提交引文（Submitted quote）**：已经随某条用户消息发出的引文。它只留在宿主队列里等待注入，**不再以任何卡片形式出现在 composer**。其可见确认就是转录区里的那条引文消息本身。

## 生命周期概念

- **一次性 / 用完即走（One-shot）**：一段待生效引用只随**下一次真实带用户文字**的消息生效一次；投递后即被清除，不长期占用、不跨会话、不持久化。
- **触发回合（Trigger turn）**：等待引用被消耗的边界。只在"真正带用户文字"的回合投递并清空；中间 assistant / tool 步骤不消耗待生效引用。
- **提交（Submit / claim）**：用户按下发送的那一刻。此刻引文从"待生效"变为"已提交"：composer 的卡片**立即消失且不再回来**，而引文本身仍留在队列里，等它的回合真正开始时投递。

## 关系

- 一个会话可有 **0..N** 条待生效引用（可累积）。
- 一条待生效引用 → 用户提交 → 成为已提交引文 → 在下一个真实用户回合投递为**引文消息** → 随即从会话中清除。
- 引文消息与用户自己的消息**是两条独立消息**，内容不互相混入；转录区顺序为「引文 → 用户自己的问题」。
- 引文的权威记录是 session log 里那条 `user/message` 事件；composer 的卡片只是提交前的暂存显示。
