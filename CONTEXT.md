# dsh-quote — 领域词汇表

本文件只记录领域术语（纯词汇，无实现细节）。实现决策见 `docs/adr/`。

> 溯源：dsh-quote 是一个 DSH 极简工具——在对话里划选一段文字，在弹出菜单里「添加到对话」，让这段文字作为**注入的上下文**随下一条消息喂给模型，方便用户接着引用的内容继续提问。引文不进入用户自己的消息正文，只作为模型可见的上下文，用完即走。

## 核心概念

- **引文（Quote）**：用户在某条对话消息（assistant / 用户自己 / tool 输出，任意）渲染正文上划选并右击选中的一段文字。它是本插件操作的原子单位。
- **添加到对话（Quote to context）**：划选菜单里的动作文案。把选中引文登记为该会话的一条"待生效引用"。
- **注入上下文（Injected context）**：引文被喂给模型时的形态——一条带插件来源、模型可读的上下文内容，**与用户消息分离**。用户消息保持干净（只含自己的问题），引文作为独立上下文让模型读到。
- **待生效引用（Pending quote）**：用户已「添加到对话」但尚未发送的一段引文，排在该会话里等待下一次用户消息。以附件卡片显示在输入框内，可移除。

## 生命周期概念

- **一次性 / 用完即走（One-shot）**：一段待生效引用只随**下一次真实带用户文字**的消息生效一次；注入后即被清除，不长期占用、不跨会话、不持久化。
- **触发回合（Trigger turn）**：等待引用被消耗的边界。只在"真正带用户文字"的回合注入并清空；中间 assistant / tool 步骤不消耗待生效引用。

## 关系

- 一个会话可有 **0..N** 条待生效引用（可累积）。
- 一条待生效引用 → 消耗于下一条真实用户消息 → 成为该回合的注入上下文 → 随即从会话中清除。
- 注入上下文与用户消息**并行存在**，二者内容不互相混入。
